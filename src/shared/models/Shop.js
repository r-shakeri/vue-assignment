export default class Shop {
    constructor(
        id,
        addressName,
        city,
        cityColor
    ) {
        this.id = id;
        this.addressName = addressName;
        this.city = city;
        this.cityColor = cityColor;
    }
}
